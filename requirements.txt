# Runtime dependencies for intData-tools shared/root code
# (delivery/, agent_plane/, dba/, roistat/).
#
# Derived from the third-party imports actually used by this code. The previous
# contents of this file were a full system `pip freeze` (certbot, PyGObject,
# python-apt, dbus-python, ...) that could not be installed into a clean venv
# and did not reflect the project's real dependencies.
#
# Per-tool MCP servers under codex/tools/* declare their own dependencies in
# their respective pyproject.toml. Optional skill tooling (e.g. TDLib-based
# telegram scripts under codex/assets) is intentionally not pinned here.
#
# Floor pins follow the repo convention (see codex/tools/*/pyproject.toml).
# Verify exact versions in a clean virtualenv before a production rollout.

requests>=2.28
PyYAML>=6.0
SQLAlchemy>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9
beautifulsoup4>=4.11
readability-lxml>=0.8.1
trafilatura>=1.6
mcp>=1.0
